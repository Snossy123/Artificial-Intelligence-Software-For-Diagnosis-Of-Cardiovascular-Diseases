import json
import pandas as pd 
import requests
url = 'https://model-2.hanim0.repl.co/Check'
payload = {}
files=[('messageFile',('Book2.csv',open('test.csv','rb'),'text/csv'))]
headers = {}
response = requests.request("POST", url, headers=headers, data=payload, files=files)
print(response.text)
# Dic =response.text
print(response.text.split(':')[1].split("\"")[1])